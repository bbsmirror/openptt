# $Id$
BBSHOME=@prefix@

TARGET=	$(BBSHOME)/etc/
FILES=	Welcome Welcome_login goodbye register registered ve.hlp Logout\
	Welcome_birth

all:

install:
	install -d $(TARGET)
	install -c -m 644 $(FILES) $(TARGET)
